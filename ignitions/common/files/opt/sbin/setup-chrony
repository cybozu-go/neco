#!/bin/sh -e

IMAGE_ID=$(/usr/bin/rkt fetch --insecure-options=image {{ MyURL }}/api/v1/assets/cybozu-chrony.aci)

mkdir -p /etc/systemd/system/chronyd.service.d
cat <<EOF >/etc/systemd/system/chronyd.service.d/env.conf
[Service]
Environment=IMAGE_ID=$IMAGE_ID
EOF

systemctl daemon-reload
